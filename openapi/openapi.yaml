
openapi: 3.0.1
info:
  title: ImbibeAI Plugin API
  description: API interface for ImbibeAI's GPT integration and learning assistant.
  version: "1.0.0"
paths:
  /hello:
    get:
      summary: Say hello
      responses:
        '200':
          description: A friendly greeting
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
